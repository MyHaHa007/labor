<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.tianruan.LaborContractUser">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Login.LoginActivity"/>

        <activity android:name=".register.RegisterActivity"
            android:theme="@style/Translucent" />

        <activity android:name=".WaitSign.WaitSignActivity"></activity>

        <!--签名页面-->
        <activity android:name="cn.tianruan.LaborContractUser.Signature.SignatureActivity"></activity>

        <!--合同详情页面-->
        <activity android:name=".ContractDetails.ContractDetailsActivity"/>

        <!--合同连接-->
        <activity android:name=".ContractDetails.ContractUrlActivity"/>

    </application>

</manifest>